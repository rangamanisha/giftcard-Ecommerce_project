# Rest Service GiftApp

#### Start the application.
1. Build the docker image.
```
 docker-compose -f docker-compose.yml build
```
2. Up docker image(run the application).
```
docker-compose -f docker-compose.yml up -d
```
